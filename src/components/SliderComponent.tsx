import { Layout } from 'antd'
import React from 'react'

const {Slider} = Layout;
const SliderComponent = () => {
  return (
    <Slider collapsed theme> </Slider>
  )
}

export default SliderComponent